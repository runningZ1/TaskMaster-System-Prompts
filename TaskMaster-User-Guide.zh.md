# TaskMaster 用户使用指南

## 目录
- [系统概述](#系统概述)
- [安装与配置](#安装与配置)
- [两种使用方式](#两种使用方式)
- [核心工作流程](#核心工作流程)
- [用户交互模式](#用户交互模式)
- [常用命令参考](#常用命令参考)
- [典型使用场景](#典型使用场景)
- [最佳实践](#最佳实践)

---

## 系统概述

**TaskMaster** 是一个专为 AI 驱动开发设计的任务管理系统，可以与 Cursor AI、Claude Code、Windsurf 等 AI 编辑器无缝集成。

### 核心特点
- 🤖 **AI 驱动**: 利用 AI 自动生成和管理任务
- 📋 **PRD 解析**: 从产品需求文档自动生成结构化任务
- 🔍 **研究模式**: 获取最新技术和最佳实践
- 🎯 **依赖管理**: 智能处理任务间的依赖关系
- 🔄 **灵活更新**: 支持任务的增量式更新
- 📊 **复杂度分析**: 自动评估任务复杂度并提供分解建议

### 支持的 AI 模型
- Anthropic Claude (推荐)
- OpenAI GPT 系列
- Google Gemini
- Perplexity AI (推荐用于研究)
- xAI Grok
- OpenRouter
- Claude Code (无需 API key)
- Codex CLI (ChatGPT 订阅用户)

---

## 安装与配置

### 方式一: MCP 集成 (推荐)

MCP (Model Control Protocol) 允许您直接从编辑器运行 TaskMaster。

#### 1. 快速安装 (Cursor 1.0+)

点击下方链接一键安装:

[![安装 TaskMaster MCP](https://cursor.com/deeplink/mcp-install-dark.svg)](https://cursor.com/en/install-mcp?name=task-master-ai&config=...)

> 💡 **提示**: 安装后需要替换占位符 API keys 为您的真实密钥

#### 2. 手动配置 MCP

根据您使用的编辑器,在相应位置创建配置文件:

**Cursor / Windsurf / Q CLI** (`mcpServers`):

```json
{
  "mcpServers": {
    "task-master-ai": {
      "command": "npx",
      "args": ["-y", "task-master-ai"],
      "env": {
        "ANTHROPIC_API_KEY": "您的_ANTHROPIC_API_密钥",
        "PERPLEXITY_API_KEY": "您的_PERPLEXITY_API_密钥",
        "OPENAI_API_KEY": "您的_OPENAI_密钥",
        // 其他 API 密钥...
      }
    }
  }
}
```

**配置文件位置**:
- **Cursor**: `~/.cursor/mcp.json` (全局) 或 `<项目>/.cursor/mcp.json` (项目)
- **Windsurf**: `~/.codeium/windsurf/mcp_config.json`
- **VS Code**: `<项目>/.vscode/mcp.json` (使用 `"servers"` 键)

#### 3. (仅 Cursor) 启用 MCP 服务器

1. 打开 Cursor 设置 (`Ctrl+Shift+J`)
2. 点击左侧 MCP 标签
3. 启用 `task-master-ai` 切换开关

#### 4. 配置 AI 模型 (可选)

在编辑器的 AI 聊天面板中,说:

```
将主模型、研究模型和备用模型分别更改为 <模型名称>
```

示例:
```
将主模型更改为 claude-code/sonnet
```

#### 5. 初始化项目

在 AI 聊天面板中:

```
在我的项目中初始化 taskmaster-ai
```

### 方式二: 命令行安装

```bash
# 全局安装
npm install -g task-master-ai

# 或在项目内安装
npm install task-master-ai

# 初始化项目
task-master init

# 或使用 npx
npx task-master init
```

### 工具加载优化

TaskMaster 支持选择性加载工具以减少上下文窗口使用:

| 模式 | 工具数 | Token 使用 | 适用场景 |
|------|-------|-----------|---------|
| `all` (默认) | 36 | ~21,000 | 完整功能集 |
| `standard` | 15 | ~10,000 | 常用任务管理 |
| `core` / `lean` | 7 | ~5,000 | 日常核心工作流 |
| 自定义 | 可变 | 可变 | 逗号分隔的工具列表 |

**配置方法**:

在 MCP 配置的 `env` 部分添加:

```json
{
  "env": {
    "TASK_MASTER_TOOLS": "standard",  // 选项: "all", "standard", "core"
    // 其他 API 密钥...
  }
}
```

---

## 两种使用方式

### 方式一: AI 聊天交互 (推荐)

通过与 AI 助手对话来管理任务,这是最自然和高效的方式。

**优势**:
- ✅ 自然语言交互
- ✅ AI 自动选择合适的命令
- ✅ 上下文感知
- ✅ 无需记忆命令语法

**示例对话**:
```
用户: 帮我解析 PRD 文档生成任务
AI: 执行: task-master parse-prd .taskmaster/docs/prd.txt

用户: 下一步我该做什么?
AI: 执行: task-master next
    显示: 任务 3 - 实现用户认证系统

用户: 帮我实现任务 3
AI: [读取任务详情并开始实现代码]
```

### 方式二: 命令行直接操作

直接使用命令行工具执行任务管理操作。

**优势**:
- ✅ 精确控制
- ✅ 可编写脚本
- ✅ 适合自动化

**基本命令**:
```bash
# 解析 PRD
task-master parse-prd prd.txt

# 列出所有任务
task-master list

# 显示下一个任务
task-master next

# 查看特定任务
task-master show 1,3,5

# 更新任务状态
task-master set-status --id=3 --status=done
```

---

## 核心工作流程

### 1. 项目启动阶段

#### 步骤 1: 准备 PRD (产品需求文档)

在 `.taskmaster/docs/prd.txt` 创建详细的 PRD 文档。

**PRD 最佳实践**:
- 📝 详细描述功能需求
- 🎯 明确技术栈和架构
- 🔧 说明具体的库和版本
- 📊 包含数据模型和 API 设计
- ✅ 定义验收标准

**示例 PRD 结构**:
```
# 项目名称: 用户认证系统

## 功能需求
1. 用户注册 (邮箱 + 密码)
2. 邮箱验证
3. 用户登录
4. JWT Token 管理
5. 密码重置

## 技术栈
- Node.js + Express
- PostgreSQL
- JWT 认证
- Nodemailer 邮件服务

## 数据库模型
...
```

#### 步骤 2: 解析 PRD 生成任务

**AI 交互方式**:
```
请解析我的 PRD 并生成任务列表
```

**命令行方式**:
```bash
task-master parse-prd .taskmaster/docs/prd.txt

# 限制生成任务数量
task-master parse-prd prd.txt --num-tasks=5

# 让 AI 根据复杂度决定任务数量
task-master parse-prd prd.txt --num-tasks=0

# 使用研究模式 (推荐)
task-master parse-prd prd.txt --research
```

**输出**: 在 `.taskmaster/tasks.json` 生成结构化任务列表

#### 步骤 3: 生成任务文件

**AI 交互**:
```
请生成独立的任务文件
```

**命令行**:
```bash
task-master generate
```

这会在 `.taskmaster/tasks/` 目录创建独立文件 (`task_001.txt`, `task_002.txt` 等)

### 2. 任务执行阶段

#### 步骤 1: 查看下一个任务

**AI 交互**:
```
下一步我应该做什么?
```

**命令行**:
```bash
task-master next
```

**输出示例**:
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 下一个任务 (任务 #3)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🏷️  标题: 实现用户认证系统
📊 状态: pending
⭐ 优先级: high
🔗 依赖: [1 ✅, 2 ✅]

📝 描述:
实现基于 JWT 的用户认证系统,包括注册、登录和密码重置功能

💡 实现细节:
1. 创建 User 模型 (email, password_hash)
2. 实现密码哈希 (bcrypt)
3. 生成和验证 JWT tokens
4. 创建认证中间件
...

建议操作:
task-master set-status --id=3 --status=in-progress
```

#### 步骤 2: 实现任务

**AI 交互**:
```
帮我实现任务 3
```

AI 会:
1. 读取任务详情
2. 查看依赖任务的实现
3. 编写代码
4. 创建测试

#### 步骤 3: 更新任务状态

**标记为进行中**:
```
请将任务 3 标记为进行中
```
或
```bash
task-master set-status --id=3 --status=in-progress
```

**标记为完成**:
```
任务 3 已完成,请更新状态
```
或
```bash
task-master set-status --id=3 --status=done
```

### 3. 任务管理阶段

#### 复杂度分析

**AI 交互**:
```
分析一下我们任务的复杂度
```

**命令行**:
```bash
# 分析所有任务
task-master analyze-complexity

# 使用研究模式
task-master analyze-complexity --research

# 设置复杂度阈值
task-master analyze-complexity --threshold=6

# 查看分析报告
task-master complexity-report
```

**输出示例**:
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 任务复杂度分析报告
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

高复杂度任务 (8-10):
  任务 5: 实现 WebSocket 实时通信 [复杂度: 9]
  → 推荐: 7 个子任务
  → 命令: task-master expand --id=5 --num=7

中等复杂度任务 (5-7):
  任务 3: 用户认证系统 [复杂度: 6]
  → 推荐: 4 个子任务

低复杂度任务 (1-4):
  任务 1: 初始化项目 [复杂度: 2]
  → 无需分解
```

#### 任务扩展 (生成子任务)

**AI 交互**:
```
任务 5 很复杂,帮我分解成子任务
```

**命令行**:
```bash
# 基于复杂度报告自动扩展
task-master expand --id=5

# 指定子任务数量
task-master expand --id=5 --num=7

# 动态决定子任务数量
task-master expand --id=5 --num=0

# 使用研究模式
task-master expand --id=5 --research

# 提供额外上下文
task-master expand --id=5 --prompt="重点关注安全性"

# 扩展所有待处理任务
task-master expand --all

# 强制重新生成已有子任务
task-master expand --all --force
```

**输出示例**:
```
✅ 任务 5 已扩展为 7 个子任务:
  5.1 设计 WebSocket 消息协议
  5.2 实现 WebSocket 服务器
  5.3 创建客户端连接管理
  5.4 实现消息队列
  5.5 添加断线重连机制
  5.6 实现消息持久化
  5.7 编写集成测试
```

#### 任务更新

**场景**: 实现过程中发现需要调整方案

**AI 交互**:
```
我们决定从 PostgreSQL 改用 MongoDB,请更新所有相关任务
```

**命令行**:
```bash
# 更新从任务 4 开始的所有任务
task-master update --from=4 --prompt="现在使用 MongoDB 代替 PostgreSQL"

# 使用研究模式
task-master update --from=4 --prompt="迁移到 MongoDB" --research
```

**更新单个任务**:
```bash
# 更新特定任务
task-master update-task --id=5 --prompt="添加 Redis 缓存层"

# 使用研究模式
task-master update-task --id=5 --prompt="集成 Redis" --research
```

**更新子任务**:
```bash
# 追加信息到子任务
task-master update-subtask --id=5.2 --prompt="添加 API 速率限制 (100请求/分钟)"
```

---

## 用户交互模式

### 模式一: 自然语言对话

TaskMaster 最强大的功能是通过自然语言与 AI 助手交互。

#### 项目初始化对话

```
用户: 我刚初始化了一个新项目,PRD 在 .taskmaster/docs/prd.txt,
      帮我设置初始任务

AI:   好的,我来帮你解析 PRD 并生成任务。
      [执行: task-master parse-prd .taskmaster/docs/prd.txt]

      我已生成 12 个任务:
      - 任务 1-3: 项目基础设置
      - 任务 4-7: 核心功能实现
      - 任务 8-10: 测试和文档
      - 任务 11-12: 部署配置

      要生成独立的任务文件吗?

用户: 好的,生成吧

AI:   [执行: task-master generate]
      ✅ 已在 .taskmaster/tasks/ 生成 12 个任务文件
```

#### 任务执行对话

```
用户: 下一步做什么?

AI:   [执行: task-master next]

      建议先做任务 1: 初始化项目仓库

      这个任务需要:
      1. 创建 Git 仓库
      2. 设置 .gitignore
      3. 初始化 npm 项目
      4. 安装基础依赖

      依赖: 无
      优先级: high

      我开始帮你实现吗?

用户: 好的,开始吧

AI:   [开始编写代码和配置文件]
```

#### 复杂任务分解对话

```
用户: 任务 5 看起来很复杂,能分解一下吗?

AI:   [执行: task-master analyze-complexity --id=5]

      任务 5 的复杂度评分是 8/10,建议分解为 6 个子任务。

      让我帮你扩展这个任务?

用户: 好的,请重点关注安全性

AI:   [执行: task-master expand --id=5 --prompt="重点关注安全性"]

      已将任务 5 分解为 6 个子任务:
      5.1 设计安全的 API 端点
      5.2 实现输入验证和净化
      5.3 配置 HTTPS 和 CORS
      5.4 实现 JWT 认证
      5.5 添加速率限制
      5.6 安全测试和漏洞扫描
```

#### 研究驱动开发对话

```
用户: 任务 8 需要集成 React Query,
      能研究一下最新的 v5 版本最佳实践吗?

AI:   [执行: task-master research "React Query v5 最佳实践" --id=8]

      我找到了以下关键信息:

      1. React Query v5 的重大变更:
         - 移除了 onSuccess/onError/onSettled 回调
         - 新增 useSuspenseQuery hook
         - 改进的 TypeScript 支持

      2. 推荐的数据获取模式:
         - 使用 queryClient.prefetchQuery 预加载
         - 启用 staleTime 减少重复请求

      3. 最佳实践:
         - 使用 Query Keys 工厂模式
         - 实现乐观更新
         - 正确处理错误边界

      要我用这些最新信息更新任务 8 吗?

用户: 好的,更新吧

AI:   [执行: task-master update-task --id=8 --prompt="基于 React Query v5 最佳实践更新" --research]

      ✅ 已更新任务 8,包含最新的 v5 实践指南
```

#### 任务状态管理对话

```
用户: 我完成了任务 3 的所有测试,可以标记为完成了

AI:   [执行: task-master set-status --id=3 --status=done]

      ✅ 任务 3 已标记为完成

      [执行: task-master next]

      下一个建议任务是任务 4: 实现用户权限系统
      这个任务依赖于任务 3 (已完成 ✅)

      要开始实现任务 4 吗?
```

### 模式二: 批量操作对话

```
用户: 显示任务 1, 3, 5 的状态

AI:   [执行: task-master show 1,3,5]

      ┌────┬──────────────────┬──────────┬──────────┐
      │ ID │ 标题             │ 状态     │ 优先级   │
      ├────┼──────────────────┼──────────┼──────────┤
      │ 1  │ 初始化项目       │ done ✅  │ high     │
      │ 3  │ 用户认证系统     │ done ✅  │ high     │
      │ 5  │ WebSocket 通信   │ pending  │ medium   │
      └────┴──────────────────┴──────────┴──────────┘

      批量操作建议:
      - 扩展所有任务: task-master expand --id=1,3,5
      - 标记为进行中: task-master set-status --id=5 --status=in-progress

用户: 把任务 5 标记为进行中

AI:   [执行: task-master set-status --id=5 --status=in-progress]
      ✅ 任务 5 已标记为进行中
```

---

## 常用命令参考

### 任务查看

```bash
# 列出所有任务
task-master list

# 按状态过滤
task-master list --status=pending
task-master list --status=in-progress
task-master list --status=done

# 包含子任务
task-master list --with-subtasks

# 显示特定任务
task-master show 3
task-master show 1,3,5
task-master show 5.2  # 子任务

# 下一个任务
task-master next
```

### 任务创建和解析

```bash
# 解析 PRD
task-master parse-prd prd.txt
task-master parse-prd prd.txt --num-tasks=5
task-master parse-prd prd.txt --research

# 添加新任务
task-master add-task --prompt="实现用户个人资料上传"
task-master add-task --prompt="..." --priority=high
task-master add-task --prompt="..." --dependencies=1,2
task-master add-task --prompt="..." --research
```

### 任务更新

```bash
# 更新多个任务
task-master update --from=4 --prompt="使用 MongoDB"
task-master update --from=4 --prompt="..." --research

# 更新单个任务
task-master update-task --id=5 --prompt="添加缓存"
task-master update-task --id=5 --prompt="..." --research

# 更新子任务
task-master update-subtask --id=5.2 --prompt="添加速率限制"
task-master update-subtask --id=5.2 --prompt="..." --research
```

### 任务扩展

```bash
# 扩展任务
task-master expand --id=5
task-master expand --id=5 --num=7
task-master expand --id=5 --prompt="关注安全"
task-master expand --id=5 --research

# 扩展所有任务
task-master expand --all
task-master expand --all --force
task-master expand --all --research

# 清除子任务
task-master clear-subtasks --id=5
task-master clear-subtasks --all
```

### 状态管理

```bash
# 设置状态
task-master set-status --id=3 --status=done
task-master set-status --id=1,2,3 --status=done
task-master set-status --id=5.1 --status=in-progress

# 可用状态
# - pending (待处理)
# - in-progress (进行中)
# - done (已完成)
# - deferred (延期)
# - cancelled (已取消)
# - review (审查中)
```

### 依赖管理

```bash
# 添加依赖
task-master add-dependency --id=5 --depends-on=3

# 移除依赖
task-master remove-dependency --id=5 --depends-on=3

# 验证依赖
task-master validate-dependencies

# 修复依赖
task-master fix-dependencies
```

### 复杂度分析

```bash
# 分析复杂度
task-master analyze-complexity
task-master analyze-complexity --threshold=6
task-master analyze-complexity --research

# 查看报告
task-master complexity-report
```

### 研究功能

```bash
# 研究技术问题
task-master research "JWT 认证最佳实践"
task-master research "React Query v5 迁移" --id=8
task-master research "WebSocket 性能优化" --files=src/websocket.js
```

### 标签管理 (Tag Management)

```bash
# 创建标签
task-master add-tag feature-auth --description="认证功能"
task-master add-tag --from-branch  # 从 Git 分支创建

# 切换标签
task-master use-tag feature-auth

# 列出标签
task-master tags
task-master tags --show-metadata

# 复制标签
task-master add-tag hotfix --copy-from-current

# 删除标签
task-master delete-tag feature-auth
```

### 任务移动

```bash
# 移动任务
task-master move --from=5 --to=3  # 任务变子任务
task-master move --from=5.2 --to=7.3  # 子任务移动

# 跨标签移动
task-master move --from=5 --from-tag=backlog --to-tag=in-progress
task-master move --from=5,6,7 --from-tag=backlog --to-tag=done --with-dependencies
```

---

## 典型使用场景

### 场景 1: 新项目启动

```
# 第 1 步: 安装和初始化
npm install -g task-master-ai
task-master init

# 第 2 步: 准备 PRD
# 编辑 .taskmaster/docs/prd.txt

# 第 3 步: 生成任务
task-master parse-prd .taskmaster/docs/prd.txt --research

# 第 4 步: 生成任务文件
task-master generate

# 第 5 步: 分析复杂度
task-master analyze-complexity --research

# 第 6 步: 查看复杂度报告
task-master complexity-report

# 第 7 步: 扩展复杂任务
task-master expand --all

# 第 8 步: 开始开发
task-master next
```

### 场景 2: 日常开发工作流

#### 早晨开始工作

**AI 对话**:
```
用户: 早上好,今天应该做什么?

AI:   早上好!让我看看当前的任务状态...
      [执行: task-master list --status=in-progress]
      [执行: task-master next]

      你昨天在做任务 5: WebSocket 实时通信 (进行中)

      当前进度:
      ✅ 5.1 设计消息协议 (已完成)
      ✅ 5.2 实现服务器 (已完成)
      ⏱️  5.3 客户端连接管理 (进行中)
      ⏱️  5.4 消息队列
      ⏱️  5.5 断线重连

      建议继续完成子任务 5.3,或者告诉我你想做其他的。
```

#### 遇到技术问题

**AI 对话**:
```
用户: 子任务 5.3 的客户端断线重连机制不太确定怎么实现,
      能研究一下最佳实践吗?

AI:   [执行: task-master research "WebSocket 断线重连最佳实践" --id=5.3]

      我找到了几种常用的重连策略:

      1. 指数退避算法
         - 第1次: 1秒后重连
         - 第2次: 2秒后重连
         - 第3次: 4秒后重连
         - 最大: 30秒

      2. 心跳检测
         - 每30秒发送 ping
         - 超时3次则认为断线

      3. 重连次数限制
         - 最多重连 10 次
         - 超过后提示用户刷新

      要我用这些信息更新子任务 5.3 吗?

用户: 好的,更新吧

AI:   [执行: task-master update-subtask --id=5.3 --prompt="实现指数退避重连 + 心跳检测"]
      ✅ 已更新子任务 5.3
```

#### 完成一天的工作

**AI 对话**:
```
用户: 我完成了子任务 5.3,今天就到这里

AI:   [执行: task-master set-status --id=5.3 --status=done]
      ✅ 子任务 5.3 已标记为完成

      今天的进度总结:
      ✅ 5.3 客户端连接管理 (今天完成)

      任务 5 总体进度: 3/7 (43%)
      ✅ 5.1, 5.2, 5.3
      ⏱️  5.4, 5.5, 5.6, 5.7

      明天建议: 继续实现 5.4 消息队列
```

### 场景 3: 技术方案调整

**情况**: 开发过程中发现需要更换技术栈

**AI 对话**:
```
用户: 我们发现 PostgreSQL 对于这个项目来说太重了,
      决定改用 MongoDB。请更新所有受影响的任务

AI:   好的,让我先看看哪些任务涉及数据库...
      [分析任务列表]

      发现以下任务受影响:
      - 任务 4: 设计数据库模式 (已完成)
      - 任务 6: 实现 CRUD 操作 (进行中)
      - 任务 7: 数据库迁移脚本 (待处理)
      - 任务 9: 数据库备份 (待处理)

      我会:
      1. 保留任务 4 (已完成的工作)
      2. 更新任务 6-9 使用 MongoDB

      执行更新吗?

用户: 好的

AI:   [执行: task-master update --from=6 --prompt="从 PostgreSQL 迁移到 MongoDB,使用 Mongoose ORM"]

      ✅ 已更新 4 个任务

      主要变更:
      - 任务 6: 现在使用 Mongoose 模型和 Schema
      - 任务 7: 改为 MongoDB 数据迁移脚本
      - 任务 9: 使用 mongodump 进行备份

      要我研究 Mongoose 最佳实践吗?

用户: 好的,研究一下

AI:   [执行: task-master research "Mongoose 最佳实践 2024" --id=6]
      [提供详细的研究结果]
```

### 场景 4: 团队协作与分支管理

**场景**: 多个功能并行开发

**AI 对话**:
```
用户: 我们团队要并行开发认证和支付两个功能,
      每个都在独立分支。怎么组织任务?

AI:   好的,我建议使用标签(tags)来管理不同功能的任务。
      让我帮你设置:

      [执行: task-master add-tag feature-auth --description="用户认证功能"]
      [执行: task-master add-tag feature-payment --description="支付系统"]

      现在你可以:

      1. 切换到认证功能:
         task-master use-tag feature-auth

      2. 切换到支付功能:
         task-master use-tag feature-payment

      每个标签有独立的任务列表,互不干扰。

      要我帮你在当前标签生成认证相关任务吗?

用户: 好的,先生成认证功能的任务

AI:   [执行: task-master use-tag feature-auth]
      [执行: task-master parse-prd .taskmaster/docs/auth-prd.txt]

      ✅ 已在 feature-auth 标签生成 8 个任务
```

### 场景 5: 复杂功能分解

**场景**: 需要实现复杂的实时通信系统

**AI 对话**:
```
用户: 我需要实现一个实时聊天系统,
      包含 WebSocket、消息持久化、在线状态等功能。
      这个任务很复杂,怎么规划?

AI:   这确实是个复杂功能,让我帮你分析和分解:

      [执行: task-master add-task --prompt="实现实时聊天系统,包含 WebSocket 通信、消息持久化、用户在线状态、消息已读回执" --priority=high]

      ✅ 已创建任务 15: 实时聊天系统

      [执行: task-master analyze-complexity --id=15]

      复杂度分析:
      - 评分: 9/10 (非常复杂)
      - 建议子任务数: 8-10
      - 涉及领域: WebSocket、数据库、缓存、状态管理

      让我帮你分解这个任务,并使用研究模式获取最佳实践:

      [执行: task-master expand --id=15 --research --prompt="重点关注可扩展性和性能"]

      已生成 9 个子任务:

      15.1 设计 WebSocket 消息协议
      15.2 实现 WebSocket 服务器 (Socket.io)
      15.3 实现客户端 WebSocket 管理器
      15.4 设计消息数据模型 (MongoDB)
      15.5 实现消息持久化服务
      15.6 实现在线状态追踪 (Redis)
      15.7 实现消息已读回执
      15.8 添加消息队列 (RabbitMQ)
      15.9 性能测试和优化

      每个子任务都包含详细的实现指导和最新的技术推荐。

      建议开发顺序:
      1. 先做 15.1, 15.4 (设计阶段)
      2. 然后 15.2, 15.3 (基础实现)
      3. 再做 15.5, 15.6, 15.7 (高级功能)
      4. 最后 15.8, 15.9 (优化)

      要查看第一个子任务的详情吗?

用户: 好的,显示 15.1

AI:   [执行: task-master show 15.1]
      [显示详细的任务信息]
```

---

## 最佳实践

### 1. PRD 编写最佳实践

**✅ 好的 PRD**:
```markdown
# 用户认证系统

## 技术栈
- Node.js 18+
- Express 4.18+
- PostgreSQL 14+
- JWT 认证

## 功能需求

### 1. 用户注册
- 邮箱 + 密码注册
- 密码强度验证 (至少 8 字符,包含大小写和数字)
- 发送验证邮件
- 使用 bcrypt 哈希密码 (salt rounds: 10)

### 2. 邮箱验证
- 生成唯一验证 token (UUID v4)
- Token 有效期 24 小时
- 验证成功后激活账户

## 数据模型

### Users 表
- id (UUID, PK)
- email (VARCHAR(255), UNIQUE)
- password_hash (VARCHAR(255))
- email_verified (BOOLEAN)
- created_at (TIMESTAMP)

## API 端点

POST /api/auth/register
- Body: { email, password }
- Response: { userId, message }

...
```

**❌ 不好的 PRD**:
```
做一个用户系统,要能注册和登录
```

### 2. 任务管理最佳实践

#### ✅ 遵循依赖关系
```
任务 1: 初始化数据库
任务 2: 创建 User 模型 (依赖: 1)
任务 3: 实现注册 API (依赖: 2)
```

#### ✅ 合理的任务粒度
```
好的粒度:
  任务: 实现用户注册 API
  子任务:
    - 创建路由处理器
    - 实现密码哈希
    - 发送验证邮件
    - 编写单元测试

不好的粒度:
  任务: 实现整个用户系统 (太大)
  任务: 添加一个变量 (太小)
```

#### ✅ 使用研究模式
```bash
# 对于新技术或不确定的实现
task-master expand --id=5 --research
task-master update-task --id=8 --prompt="..." --research
task-master research "React Query v5 最佳实践" --id=8
```

### 3. 工作流程最佳实践

#### ✅ 每日工作流

```bash
# 早晨
task-master next  # 查看今天的任务
task-master show 5  # 查看任务详情

# 开始工作
task-master set-status --id=5 --status=in-progress

# 遇到问题时
task-master research "WebSocket 重连策略" --id=5

# 完成后
task-master set-status --id=5 --status=done
task-master next  # 查看下一个任务
```

#### ✅ 定期维护

```bash
# 每周
task-master analyze-complexity  # 分析任务复杂度
task-master validate-dependencies  # 验证依赖关系
task-master list --status=deferred  # 检查延期任务

# 发现问题时
task-master fix-dependencies  # 修复依赖问题
```

### 4. 团队协作最佳实践

#### ✅ 使用标签管理并行开发

```bash
# 为每个功能创建标签
task-master add-tag feature-auth
task-master add-tag feature-payment
task-master add-tag bugfix-hotfix

# 切换上下文
task-master use-tag feature-auth
task-master list  # 只看认证相关任务
```

#### ✅ 任务命名规范

```
✅ 好的命名:
- 实现用户注册 API
- 添加 JWT 认证中间件
- 修复登录失败 bug

❌ 不好的命名:
- 做一些工作
- 更新代码
- 修复
```

### 5. 性能优化最佳实践

#### ✅ 选择合适的工具加载模式

```json
{
  "env": {
    // 日常开发: 使用 core 模式
    "TASK_MASTER_TOOLS": "core",

    // 复杂项目: 使用 standard 模式
    // "TASK_MASTER_TOOLS": "standard",

    // 完整功能: 使用 all 模式
    // "TASK_MASTER_TOOLS": "all"
  }
}
```

#### ✅ 使用研究模式时机

```
✅ 适合使用研究模式:
- 不熟悉的新技术
- 需要最新最佳实践
- 技术选型决策
- 复杂的架构设计

❌ 不需要研究模式:
- 简单的 CRUD 操作
- 已经很熟悉的技术
- 标准的项目配置
```

### 6. 错误处理和恢复

#### 遇到任务冲突

**AI 对话**:
```
用户: 我合并主分支后 tasks.json 有冲突,
      我的分支创建了任务 10-12,
      队友的分支也创建了任务 10-15

AI:   让我帮你解决这个冲突。我会把你的任务重新编号:

      [执行: task-master move --from=10 --to=16]
      [执行: task-master move --from=11 --to=17]
      [执行: task-master move --from=12 --to=18]

      ✅ 已解决冲突,你的任务现在是 16-18
```

#### 任务顺序错误

```bash
# 验证依赖关系
task-master validate-dependencies

# 自动修复
task-master fix-dependencies
```

---

## 常见问题 (FAQ)

### Q: 如何选择主模型?

**A**: 推荐选择:
- **日常开发**: Claude 3.5 Sonnet (平衡性能和成本)
- **复杂任务**: Claude 3.7 Opus (最强能力)
- **无 API key**: Claude Code (免费,需要 Claude Code CLI)
- **研究模式**: Perplexity AI (最新信息)

### Q: 什么时候使用研究模式?

**A**: 在以下情况使用 `--research` 标志:
- 使用不熟悉的技术
- 需要最新的最佳实践
- 技术选型和架构决策
- 遇到复杂的技术问题

### Q: 如何处理大型项目?

**A**: 大型项目建议:
1. 使用标签(tags)分隔不同功能模块
2. 定期分析任务复杂度
3. 将复杂任务分解为子任务
4. 使用 `core` 工具模式减少 token 使用

### Q: 可以在多个项目中使用吗?

**A**: 可以,TaskMaster 会在每个项目根目录创建 `.taskmaster/` 文件夹,各项目的任务完全独立。

### Q: 如何备份任务?

**A**: 任务存储在 `.taskmaster/tasks.json`,建议:
1. 使用 Git 管理
2. 定期备份 `.taskmaster/` 目录
3. 使用标签复制: `task-master add-tag backup --copy-from-current`

### Q: 与 Cursor/Windsurf/VS Code 有什么区别?

**A**: TaskMaster 是任务管理系统,与这些编辑器配合使用:
- **Cursor/Windsurf**: 通过 MCP 集成,AI 助手可直接调用 TaskMaster
- **VS Code**: 支持 MCP,但需要手动配置
- **命令行**: 可独立使用,不依赖特定编辑器

---

## 总结

TaskMaster 是一个强大的 AI 驱动任务管理系统,核心优势在于:

1. **自然交互**: 通过 AI 对话管理任务,无需记忆复杂命令
2. **智能分解**: 自动分析任务复杂度并提供分解建议
3. **研究增强**: 获取最新技术和最佳实践
4. **灵活管理**: 支持依赖管理、标签分组、跨标签移动
5. **无缝集成**: 与主流 AI 编辑器完美配合

**开始使用**:
```bash
# 1. 安装
npm install -g task-master-ai

# 2. 初始化
task-master init

# 3. 准备 PRD
# 编辑 .taskmaster/docs/prd.txt

# 4. 开始工作
# 在 AI 聊天中说: "帮我解析 PRD 并生成任务"
```

**获取帮助**:
- 📚 [官方文档](https://docs.task-master.dev)
- 💬 [Discord 社区](https://discord.gg/taskmasterai)
- 🐛 [GitHub Issues](https://github.com/eyaltoledano/claude-task-master/issues)

**祝你开发愉快!** 🚀
